[project]
name = "pa-whatsapp"
version = "0.1.0"
description = "PA WhatsApp integration (future)"
requires-python = ">=3.11"
dependencies = ["pa-core"]

[project.scripts]
pa-whatsapp = "pa_whatsapp.cli:main"

[tool.uv.sources]
pa-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
